apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "springboot-local.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "springboot-local.name" . }}
    helm.sh/chart: {{ include "springboot-local.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "springboot-local.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
